import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "HRMS Pro | Enterprise Workforce & UK Compliance System",
  description: "Modern Human Resource Management System with automated Right to Work (RTW) and Immigration Visa compliance tracking.",
};

export default function RootLayout({ children }) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col font-sans bg-zinc-100 dark:bg-zinc-950 text-zinc-900 dark:text-zinc-100">{children}</body>
    </html>
  );
}
