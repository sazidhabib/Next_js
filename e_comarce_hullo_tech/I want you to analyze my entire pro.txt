I want you to analyze my entire project and generate a single `.md` file that will serve as a **permanent project blueprint**. This file will be read by AI (or me) before ANY future task (new feature, bug fix, module update) so that the AI understands the full project structure WITHOUT re-reading the codebase each time.

## Requirements for the blueprint:

### 1. Project Overview
- Tech stack (language, framework, database, etc.)
- Entry point(s) (main file, index, app root)
- Purpose of project (1-2 sentences)

### 2. Complete File Tree with Purpose
For EVERY file/folder in `/src`, `/lib`, `/components`, `/routes`, `/utils`, etc.:

```markdown
📁 src/
  📄 main.py - Flask app entry, route definitions
  📁 models/
    📄 user.py - User model, CRUD operations, validation
    📄 post.py - Blog post model, relationships
  📁 utils/
    📄 db.py - Database connection pool, query helpers
    📄 auth.py - JWT token generation/verification
  ...

### 3. Dependency Map
Which files import/use which other files:

main.py → models/user.py, utils/db.py, utils/auth.py
user.py → utils/db.py, utils/validator.py
post.py → models/user.py, utils/db.py

### 4. Key Functions & Exports (only critical ones)

| File | Function/Class | Purpose | Called by |
|------|---------------|---------|-----------|
| utils/auth.py | generate_token(user_id) | Returns JWT | main.py (login route) |
| models/user.py | find_by_email(email) | Returns user dict | auth.py, admin.py |

### 5. Data Flow (where data enters, transforms, exits)

[API Request] → main.py (route) → validator.py → model.py → db.py → [Response]

### 6. Configuration & Environment Variables

.env file or config.py contains:
- DB_URL (used in utils/db.py)
- JWT_SECRET (used in utils/auth.py)
- DEBUG_MODE (used in main.py)

### 7. Common Modification Points (for future changes)

TO ADD NEW ROUTE: modify main.py + add to models/
TO CHANGE AUTH LOGIC: modify utils/auth.py
TO ADD DATABASE FIELD: modify models/X.py + migration file

### 8. Testing Structure (if exists)

tests/
  test_auth.py → tests login/signup (depends on utils/auth.py)
  test_models.py → tests user/post models

  save this file with this name: PROJECT_BLUEPRINT.md (this file is for future use for ai)




